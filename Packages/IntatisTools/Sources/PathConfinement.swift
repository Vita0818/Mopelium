// PathConfinement moved to IntatisCore so IntatisPermission can also use it
// without depending on IntatisTools. Tools reference `PathConfinement` via
// `import IntatisCore`. This file is intentionally left as a pointer only.
