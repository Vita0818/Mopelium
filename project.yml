# XcodeGen spec — generates the Xcode project that builds the real .app bundles.
#
#   brew install xcodegen      # one-time
#   xcodegen generate          # creates Intatis.xcodeproj
#   open Intatis.xcodeproj     # pick the IntatisMac or IntatisiOS scheme, then Run
#
# The App targets link the Swift Package products in this repo. The iOS app links
# ONLY the chat subset (no Tools / Permission / AgentKernel / Cowork).

name: Intatis

options:
  bundleIdPrefix: com.Vita0818
  developmentLanguage: en
  deploymentTarget:
    macOS: "26.0"
    iOS: "26.0"
  createIntermediateGroups: true
  localPackagesGroup: "Internal Modules"
  postGenCommand: "sh scripts/hide-xcode-package-schemes.sh"

packages:
  Intatis:
    path: .

settings:
  base:
    SWIFT_VERSION: "5.9"
    MARKETING_VERSION: "0.36"
    CURRENT_PROJECT_VERSION: "36"
    CODE_SIGN_STYLE: Automatic
    GENERATE_INFOPLIST_FILE: "NO"
    # To run on a device or archive for distribution, set your team:
    # DEVELOPMENT_TEAM: ABCDE12345

targets:

  # ---- macOS DeveloperID app: Chat / Code / Cowork + stdio/HTTP MCP client ----
  IntatisMac:
    type: application
    platform: macOS
    sources:
      - Apps/IntatisMac/Sources
      - Apps/IntatisMac/Resources
      - path: Intatis.icon
        buildPhase: resources
      - path: Apps/SharedResources
        buildPhase: resources
      - path: NOTICE.md
        buildPhase: resources
      - path: ThirdPartyNotices
        type: folder
        buildPhase: resources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.Vita0818.IntatisMac
        # Let Xcode synthesize Info.plist (injects CFBundleExecutable etc.).
        GENERATE_INFOPLIST_FILE: "YES"
        INFOPLIST_KEY_CFBundleDisplayName: Mopelium
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.developer-tools
        INFOPLIST_KEY_NSHumanReadableCopyright: "Copyright © 2026 Intatis contributors. See NOTICE.md."
        INFOPLIST_KEY_NSMicrophoneUsageDescription: "Mopelium uses the microphone to turn voice into editable message drafts."
        ASSETCATALOG_COMPILER_APPICON_NAME: Intatis
        CODE_SIGN_ENTITLEMENTS: Apps/IntatisMac/IntatisMac.DeveloperID.entitlements
        ENABLE_HARDENED_RUNTIME: "YES"
        ENABLE_RESOURCE_ACCESS_AUDIO_INPUT: "YES"
        # The separately declared IntatisMacAppStore target is the only
        # App-Store profile; do not turn this stdio-capable target into one by
        # swapping entitlements.
    dependencies:
      - package: Intatis
        product: IntatisCore
      - package: Intatis
        product: IntatisProtocol
      - package: Intatis
        product: IntatisProviders
      - package: Intatis
        product: IntatisConversation
      - package: Intatis
        product: IntatisArtifacts
      - package: Intatis
        product: IntatisMultimodal
      - package: Intatis
        product: IntatisSharedUI
      - package: Intatis
        product: IntatisTools
      - package: Intatis
        product: IntatisPermission
      - package: Intatis
        product: IntatisAgentKernel
      - package: Intatis
        product: IntatisSkills
      - package: Intatis
        product: IntatisCowork
      - package: Intatis
        product: IntatisMCP
      - package: Intatis
        product: IntatisMCPStdio

  # ---- macOS App Store: full UI, remote MCP HTTP only, no stdio linkage ----
  IntatisMacAppStore:
    type: application
    platform: macOS
    sources:
      - Apps/IntatisMac/Sources
      - path: Apps/SharedResources
        buildPhase: resources
      - path: NOTICE.md
        buildPhase: resources
      - path: ThirdPartyNotices
        type: folder
        buildPhase: resources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.Vita0818.IntatisMac
        PRODUCT_NAME: Intatis
        GENERATE_INFOPLIST_FILE: "YES"
        INFOPLIST_KEY_CFBundleDisplayName: Mopelium
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.developer-tools
        INFOPLIST_KEY_NSHumanReadableCopyright: "Copyright © 2026 Intatis contributors. See NOTICE.md."
        CODE_SIGN_ENTITLEMENTS: Apps/IntatisMac/IntatisMac.AppStore.entitlements
        SWIFT_ACTIVE_COMPILATION_CONDITIONS: "$(inherited) INTATIS_MAC_APP_STORE"
    dependencies:
      - package: Intatis
        product: IntatisCore
      - package: Intatis
        product: IntatisProtocol
      - package: Intatis
        product: IntatisProviders
      - package: Intatis
        product: IntatisConversation
      - package: Intatis
        product: IntatisArtifacts
      - package: Intatis
        product: IntatisMultimodal
      - package: Intatis
        product: IntatisSharedUI
      - package: Intatis
        product: IntatisTools
      - package: Intatis
        product: IntatisPermission
      - package: Intatis
        product: IntatisAgentKernel
      - package: Intatis
        product: IntatisSkills
      - package: Intatis
        product: IntatisCowork
      - package: Intatis
        product: IntatisMCP

  # ---- iOS app: chat-only SUBSET (deliberately no workspace stack) ----
  IntatisiOS:
    type: application
    platform: iOS
    sources:
      - Apps/IntatisiOS/Sources
      - Apps/IntatisiOS/Resources
      - path: Intatis.icon
        buildPhase: resources
      - path: Apps/SharedResources
        buildPhase: resources
      - path: NOTICE.md
        buildPhase: resources
      - path: ThirdPartyNotices
        type: folder
        buildPhase: resources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.Vita0818.Intatis
        TARGETED_DEVICE_FAMILY: "1,2"
        # Let Xcode synthesize Info.plist (injects CFBundleExecutable etc.).
        GENERATE_INFOPLIST_FILE: "YES"
        INFOPLIST_KEY_CFBundleDisplayName: Mopelium
        INFOPLIST_KEY_UILaunchScreen_Generation: "YES"
        INFOPLIST_KEY_NSMicrophoneUsageDescription: "Mopelium uses the microphone for voice input transcription."
        ASSETCATALOG_COMPILER_APPICON_NAME: Intatis
    dependencies:
      - package: Intatis
        product: IntatisCore
      - package: Intatis
        product: IntatisProtocol
      - package: Intatis
        product: IntatisProviders
      - package: Intatis
        product: IntatisConversation
      - package: Intatis
        product: IntatisArtifacts
      - package: Intatis
        product: IntatisMultimodal
      - package: Intatis
        product: IntatisSharedUI

schemes:
  IntatisMac:
    build:
      targets:
        IntatisMac: all
    run:
      config: Debug
    management:
      shared: true
      orderHint: 0
      isShown: true

  IntatisMacAppStore:
    build:
      targets:
        IntatisMacAppStore: all
    run:
      config: Debug
    management:
      shared: true
      orderHint: 1
      isShown: true

  IntatisiOS:
    build:
      targets:
        IntatisiOS: all
    run:
      config: Debug
    management:
      shared: true
      orderHint: 2
      isShown: true
