# PROJECT_MAP

最近自查日期：2026-07-26

本文描述当前仓库结构。判断依据来自 `Package.swift`、`project.yml`、`Makefile`、源码、测试文件和脚本。

## 2026-07-26 活动构建基线

- `Apps/`、`Packages/`、`Vendor/`、根 SwiftPM manifest 与 XcodeGen 配置已采用 `References/Intatis` 记录的 v0.26 功能/UI 基线，但活动代码已完整改为 Mopelium 产品、模块、命令、配置与环境变量命名；构建图不引用 `References/Intatis/`。
- 生产 API key 只从环境变量解析。provider catalog、UserDefaults 和 JSON/JSONC 配置只保存变量名或 `{env:NAME}`；旧 file/auth/providerConfig/Keychain 引用只保留 wire 兼容，生产 resolver 归一或 fail closed。
- 面向 AI Agent 的 provider/model/variant 配置写入与 Cowork per-agent 绑定 runbook 已固化在 `docs/AI_PROVIDER_MODEL_CONFIGURATION.md`；配置定义池与 session durable binding 必须分层处理。

## 2026-07-23 UI 活跃状态接线

- `Packages/MopeliumConversation/Sources/SessionActivityHistoryStore.swift` 是 app-facing recent-session recency projection：一次读取 EventLog，同时统计 event count，并 newest-first 查找 durable `turn_outcome`；只有 legacy session 回退 assistant/agent completion。file size/mtime 只作为 cache invalidation signature，`SessionHistoryStore` 的文件 mtime 不再是 UI 排序权威。
- `Apps/MopeliumMac/Sources/SessionRuntimeManager.swift` 继续持有 exact session runtime，并直接组合各 ViewModel 的 published data-plane activity，发布 active→idle 的低频 settlement；`MopeliumMacRootView.swift` 只重扫该 settlement 所属的 Chat / Code / Cowork 列表。macOS 与 iOS 的 `AppConfig` / `IOSConfig` 都通过 Conversation projection 取得同一排序，iOS 也在 Chat busy→idle 后刷新 history。
- `Packages/MopeliumSharedUI/Sources/ThreadSurfaces.swift` 负责 composer 唯一主操作位的 Send↔native destructive Stop 切换，以及 phase-local `MopeliumThinkingElapsedLabel`；Mac Chat、共享 Code / Cowork 接入同一 Stop 几何与取消入口。

## 目录结构总览

```text
Mopelium/
├── .build/            SwiftPM 构建产物（gitignored）
├── .git/              Git 仓库（remote: github.com/Vita0818/Mopelium；活动实现采用重命名后的 v0.26 功能基线，project.yml 的 MARKETING_VERSION 仍标 0.12）
├── .gitattributes     LF 规范化
├── .gitignore         忽略 .build、Mopelium.xcodeproj、*.env、.mopelium 本地运行/受管 worktree 状态
├── .swiftpm/          SwiftPM 缓存
├── AGENTS.md          项目常驻上下文与操作协议入口
├── Apps/              app target
│   ├── SharedResources/ macOS/iOS 共用 English + zh-Hans String Catalog
│   ├── MopeliumMac/    全量 macOS app（默认 DeveloperID/non-sandbox workbench）+ entitlements
│   ├── MopeliumiOS/    Chat-only iOS app（7-product 子集）
│   └── mopelium-cli/   CLI
├── ARCHITECTURE.md    Mopelium 架构设计（draft-0，2026-06-11，中文）
├── Makefile           build/test/release/install/app 便利 target
├── NOTICE.md          项目来源、当前上游采用状态 + 第三方依赖声明
├── ThirdPartyNotices/ Markdown / syntax / math 完整分发声明
├── Vendor/            仓内维护的第三方派生源码；当前含 SwiftStreamingMarkdown thin derivative + 相邻许可证/patch ledger
├── Package.swift      SwiftPM manifest（11 lib + CLI + 11 test target）
├── Packages/          11 个库模块（各 Sources/，SharedUI 现有渲染 Tests）
├── References/Intatis/ 固定 v0.26 上游源码快照（只读参考，不进入任何 target）
├── README.md          Mopelium readme
├── docs/              项目状态/架构/测试/禁区说明 + Cowork 设计文档
├── scripts/           构建、验证和受控 renderer watchdog 脚本
└── project.yml        XcodeGen spec（生成 Mopelium.xcodeproj）
```

## Target / 模块

### Mopelium 库 target（11）+ 内部 C helper

| Target | 类型 | 依赖 | 职责 |
|---|---|---|---|
| `MopeliumCore` | lib | — | 类型化 ID、错误、工作区路径约束、平台能力信封、会话类型、`SessionHistoryStore`、session-owned `SessionWorkspaceAccessStore` |
| `MopeliumProtocol` | lib | Core | 结构化事件/线协议词汇（Event/Envelope/JSONRPC/Command/CoworkEvents/Goal/WorkTask/ContinuationRun/TaskGoalEvents/ModelHistory/SessionStateEvents/InferenceProfile/PermissionIntent/PermissionReview/ToolAuthorization/ToolExecution/MultimodalEvents/TurnStats/JSONValue） |
| `MopeliumProviders` | lib | Core, Protocol | OpenAI-compatible 模型访问、Chat/Code lossless model options、Cowork 显式 durable-options schema、exact per-agent inference catalog/resolver 与只读配置展示投影（ProviderRegistry/InferenceCatalog/InferenceCatalogStore/Capability/Endpoints/ChatProvider/OpenAIWireProvider/OpenAIToolCalling/ModelConfigurationPresentation/SSE/HTTPDataClient/ImageGeneration/Transcription/VideoGeneration/ToolCalling） |
| `MopeliumArtifacts` | lib | Core, Protocol | 文件-backed blob 存储 + JSON 索引 |
| `MopeliumConversation` | lib | Core, Protocol, Providers, Artifacts | 事件溯源会话 + UI 投影（跨进程锁定、multi-event WAL 与 checked replay 的 EventLog JSONL / schema-v2 `SessionProjectionStore` / ChatLoop / GoalInputParser / Projection / CodeProjection / Cowork settings/Goal/WorkTask/ContinuationRun/tool-execution recovery projection / TurnStatsProjection） |
| `MopeliumPTYLauncher` | internal C helper | libc / macOS `forkpty` | 为 managed terminal 建立真实 controlling PTY；child 在 fork 后只执行 C/POSIX signal/FD/chdir/exec，CLOEXEC error pipe 把启动错误返回父进程；非 macOS 返回不支持 |
| `MopeliumTools` | lib | Core, Protocol, PTYLauncher | 哑工具执行器与 task/goal host 接口（ToolProtocol/TaskGoalManagement/FileTools/PatchTool/PathConfinement/ShellGit/TerminalTools/DocumentMediaTools/BrowserTools）；`ToolRegistration` / `ToolRegistry` 是 schema、concrete tool、canonical permission、lease resolver、semantic preview 与 executor 的单一事实源 |
| `MopeliumPermission` | lib | Core, Protocol, Providers | 3 层权限门（PermissionTypes/PermissionEngine/DeterministicPolicyGate/ModelPermissionReviewer/SecretScanner） |
| `MopeliumAgentKernel` | lib | Core, Protocol, Providers, Tools, Permission, Conversation, Artifacts | 共享 headless 单 agent runtime（AgentRuntime/RuntimeEnvironmentManifest/AgentLoop/ContextBuilder/ContextProjector/AgentModelHistoryProjector/AgentExecutionBudget/PermissionResponder） |
| `MopeliumCowork` | lib | Core, Protocol, Providers, Tools, Permission, Conversation, AgentKernel | 多 agent 编排与 Goal 控制面（Orchestrator/AgentScheduler/MessageBus/Mediator/AgentRegistry/WorkTaskTools/GoalTools/GoalVerifierControlPlane/CoordinatorTools/AgentPermissionResponder/PermissionReviewControlPlane） |
| `MopeliumMultimodal` | lib | Core, Protocol, Providers, Artifacts, Conversation | 图像/视频/转写 → artifacts |
| `MopeliumSharedUI` | lib | Core, Protocol, Providers, Conversation, Artifacts, `Vendor/SwiftStreamingMarkdown` thin derivative（Microsoft v0.6.0 basis；传递 exact iosMath 2.5.0，仅 iOS/macOS） | 跨平台 SwiftUI；`MessageRendering` 以 `.microsoft` / `.plainSafe` 做产品熔断，rich 结构/原生布局与 code-aware 单美元行内公式由经审计的 Microsoft 派生包负责；iosMath 只提供 Apple-native TeX parse/layout，plain-safe 完全绕开二者。公式通过两平台 TextKit 2 live `MTMathUILabel` attachment 展示，1024×256-point preflight、semantic appearance 与 Dynamic Type revision 共同控制 literal fallback/更新，不保留 raster cache。Mopelium 只做 64 KiB admission、process-wide 1-running/32-pending latest-only Markdown parse permits、每 view 最新 raw revision、50 ms incomplete parse debounce、100 ms fixed-window raw leading/trailing projection、stale publication guard与安全链接。`ExecutionTracePresentation.swift` 另在 Code/Cowork 展示边界默认隐藏 verbose tool/patch/note/agent-to-agent trace 和由 `CodeProjection` 标记的同-task exact `task_completed` 回答镜像，并由后台启动参数/环境变量 opt in 恢复；它不改变 EventLog 或 durable task settlement。当前仍不分发语法高亮、远程 Markdown 图片或 block math |

### App target

| Target | 类型 | 平台 | Bundle ID | 链接 |
|---|---|---|---|---|
| `MopeliumMac` | application | macOS 26+ | `com.Vita0818.MopeliumMac` | 全部 11 个 product；默认 DeveloperID/non-sandbox workbench |
| `MopeliumiOS` | application | iOS 26+ | `com.Vita0818.Mopelium` | 7 个 chat 子集 product（无 Tools/Permission/AgentKernel/Cowork） |
| `mopelium-cli` | executable | CLI（macOS/Linux） | — | Core, Protocol, Providers, Conversation, Tools, Permission, AgentKernel, Cowork |

### 测试 target（11）— `Packages/<Mod>/Tests`

`MopeliumCoreTests`、`MopeliumProtocolTests`（+V02/V03/V04/InferenceProfile）、`MopeliumProvidersTests`（+Multimodal/ToolCalling/InferenceCatalog/StoreResolver）、`MopeliumArtifactsTests`、`MopeliumConversationTests`（+Code）、`MopeliumToolsTests`、`MopeliumPermissionTests`（+Reviewer）、`MopeliumAgentKernelTests`、`MopeliumCoworkTests`（含 `AutomaticPermissionReviewTests` / `PerAgentInferenceProfileTests`）、`MopeliumMultimodalTests`、`MopeliumSharedUITests`（`MessageRendererModeTests` / `MessageRenderingTests` / `CoworkInferencePresentationTests` / `ExecutionTracePresentationTests`）。`swift test` 无头。

## 关键文件

- 入口：`Apps/MopeliumMac/Sources/MopeliumMacApp.swift`、`Apps/MopeliumiOS/Sources/MopeliumiOSApp.swift`、`Apps/mopelium-cli/Sources/MopeliumCLI.swift`
- App 本地化：`Apps/SharedResources/Localizable.xcstrings` 由 `project.yml` 同时加入 MopeliumMac / MopeliumiOS 主 bundle，source/development language 为 English，支持 `en` / `zh-Hans`；`Packages/MopeliumSharedUI/Sources/MopeliumLocalization.swift` 为普通 `String` 展示值提供 `Bundle.main` 查表与 English fallback。iOS 系统设置页另读 `Apps/MopeliumiOS/Resources/Settings.bundle/{en,zh-Hans}.lproj/Root.strings`，生成式 Info.plist 的麦克风说明读 `Apps/MopeliumiOS/Resources/{en,zh-Hans}.lproj/InfoPlist.strings`。语言由系统/App Language 在启动时选择，不持久化 Mopelium 自有语言偏好。
- Per-agent inference profile：`Packages/MopeliumCore/Sources/IDs.swift`（profile/connection exact ID + revision）、`Packages/MopeliumProtocol/Sources/InferenceProfile.swift`（safe `AgentInferenceBinding`）、`Packages/MopeliumProviders/Sources/InferenceCatalog.swift`（immutable connection/profile definitions、显式 allowlisted durable-options schema、reconcile、含 safe route/trust/egress 的 exact resolve）、`InferenceCatalogStore.swift`（owner-only versioned store、corruption/schema/permission fail closed、稳定 sidecar 上的进程内 + Darwin/Linux 跨进程独占锁、锁内 revision allocation、原子替换）、`ProviderRegistry.swift`（同一 resolution 原子返回 binding/model/provider；当前只实例化 OpenAI-compatible wire）、`ProviderURLSession.swift`（provider traffic 禁止 HTTP redirect）、`OpenAIWireProvider.swift` / `OpenAIToolCalling.swift`（所有 Chat/Agent request 移除 config `stream_options`/多候选字段并固定 `n = 1`）、`Packages/MopeliumAgentKernel/Sources/Agent.swift` 与 `Packages/MopeliumProtocol/Sources/{Task,CoworkEvents,TurnStats,ToolAuthorization}.swift`（additive optional binding/frozen invocation/安全归因；diagnostic URL + secret redaction）、`Packages/MopeliumCowork/Sources/Orchestrator.swift`（strict attach/restore/invocation、只接受 atomic resolved tuple 的 shipping runtime、host-approved explicit profile、exact spawn inheritance、delegate target snapshot、catalog update/admission lock、resolver suspension 后 revalidation、GUI local-admission/execution-readiness split、CLI control-plane startup gate 与 unresolved-worker invocation isolation、host-only idle durable rebind、next-main submission/live-binding root admission guard）。完整契约见 `docs/PER_AGENT_INFERENCE_PROFILES.md`。
- Cowork 链路：`Packages/MopeliumCowork/Sources/Orchestrator.swift`（生产 `runtime` session writer lease、fresh empty session 固定 `@main` restricted bootstrap admission + admission-wait 后 exact profile/empty-session recheck、普通 reviewed attach + allow 后 catalog snapshot/exact profile revalidation、真实 root AgentInvocation、WorkTask manager、scoped Goal/run/work-task binding、`delegate_task` review 前 exact-target preflight、allow 后 target/lease fingerprint revalidation 与 approved-snapshot execution、单审批 `spawn_agent` admission batch、permission reviewer 逐 generation exact provider factory、bounded scheduler/watchdog、non-idempotent crash reconciliation、durable-first detach/revoke、恢复/取消/超时/重试、mailbox wake、lease lifecycle、Task Report、idle recycle）、`GoalRuntimeController.swift`（普通 turn run scope、durable Goal create/edit/pause/resume/clear、host continuation、scheduler barrier、checkpoint/recovery、冷启动 reconcile-only pause、显式 Resume、budget/usage/no-progress/blocked policy）、`AgentScheduler.swift`（queue/claim/single-flight/snapshot/mailbox）、`WorkTaskTools.swift`（`task_create/update/get/list`）、`GoalTools.swift`（`create_goal/get_goal/update_goal`）、`GoalVerifierControlPlane.swift`（独立 no-tools Goal audit）、`AgentPermissionResponder.swift`、`PermissionReviewControlPlane.swift`（reviewer 独立 FIFO/single-flight/timeout/soft usage warning/typed allow-deny failure、request generation、首 terminal/late-result guard、fresh-provider recovery）、`CommunicationDelegationTools.swift`、`CoordinatorTools.swift`、`MessageBus.swift`、`Mediator.swift`、`AskAgentTool.swift`（typed success/failure，admission/Mediator failure 不得伪装成功）
- Cowork 四层协议/投影：`Packages/MopeliumProtocol/Sources/Goal.swift`（durable user objective + audit）、`WorkTask.swift`（可见计划节点 + revision/DAG/agent-reported evidence）、`ContinuationRun.swift`（host continuation checkpoint）、`TaskGoalEvents.swift`（四层追加事件）、`Task.swift` / `TaskGraph.swift`（保留的 AgentInvocation execution layer 与可选 Goal/run/WorkTask binding）、`CoworkEvents.swift`（含 mailbox consumed/discarded 生命周期）、`Leases.swift`、`PermissionIntent.swift`、`Event.swift` / `Envelope.swift`；`Packages/MopeliumConversation/Sources/CoworkProjection.swift` 折叠 Goal/WorkTask/ContinuationRun、执行任务/agent roster/mailbox/owner/report/attempt/retry 与未决 tool execution，`EventLog.swift` 负责 append-only replay、cross-process flock、multi-event WAL/父目录同步、session identity/known payload checked replay、session writer lease 与 crash-tail 防护。
- Agent 内核：`Packages/MopeliumAgentKernel/Sources/AgentRuntime.swift`（Code/Cowork 共用的 headless registry/permission/loop 工厂与 history policy 透传）、`AgentLoop.swift`（严格 completion/finish reason、稳定 `@main` 的 durable user/assistant/function-call/output 写入与恢复、不可变 `ResolvedToolAuthorization`、reviewer reason/source 保真、跨重启 side-effect completion evidence、audit fail-closed、durable prepare 前 async execution revalidation + await 后复核、tool execution ticket、workspace lease、并行 collaboration calls、exact denied-call circuit breaker、会话名称在 authorization 前 secret scan 且 durable raw args redaction）、`AgentModelHistoryProjector.swift`（strict direct history、call/output normalize、latest attempt/invocation 选择）、`AgentThreadHistoryProjector.swift`（legacy completed root U/A bridge）、`EventLogSessionNamingService.swift`（宿主绑定当前 session/kind、execution-ID 幂等 rename 与 verified commit 通知）、`AgentExecutionBudget.swift`（dispatch 前 reservation 的共享 soft token meter）、`Agent.swift`、`ContextBuilder.swift`（`RuntimeEnvironmentManifest` + stable-main/task-scoped history policy + untrusted user-role context）、`ContextProjection.swift`（task-scoped bounded projection与 direct model/audit 去重）、`PermissionResponder.swift`、`ProviderImageGenerationToolService.swift`
- Stable-main 模型历史协议/回归：`Packages/MopeliumProtocol/Sources/ModelHistory.swift`、`Event.swift`、`Envelope.swift`；`Packages/MopeliumProtocol/Tests/ModelHistoryProtocolTests.swift`、`Packages/MopeliumAgentKernel/Tests/ModelHistoryProjectionTests.swift`、`ModelHistoryAgentLoopTests.swift`，并与 `SubmittedIntentHistoryTests.swift`、`ContextProjectionTests.swift` 和 Cowork main continuity / worker isolation 回归组合运行。
- `model_history_item` 展示边界：`Packages/MopeliumConversation/Sources/Projection.swift`、`CodeProjection.swift`、`CoworkProjection.swift` 对该 provider-facing event 执行 no-op；用户可见消息仍来自原消息事件，不会因恢复事实链多渲染一份 assistant/tool 气泡。
- Cowork 编排可靠性回归：`Packages/MopeliumCowork/Tests/OrchestrationReliabilityTests.swift`、`SchedulerMailboxTests.swift`、`TaskGraphSchedulerIntegrationTests.swift`、`ToolRegistryLeaseTests.swift`、`PermissionReviewControlPlaneTests.swift`、`AutomaticPermissionReviewTests.swift`、`AgentInvocationNonRecursiveTests.swift`、`SpawnAgentPermissionTests.swift`；per-agent inference 回归：`Packages/MopeliumProtocol/Tests/InferenceProfileProtocolTests.swift`、`Packages/MopeliumProviders/Tests/InferenceCatalogTests.swift`（durable schema 与 safe route/trust/egress mismatch）、`MopeliumProvidersTests.swift` / `InferenceCatalogStoreResolverTests.swift`（unconditional single-candidate request clamp 与 HTTP redirect no-follow；后者另含 32 个并发 reconciler 的 revision 保留/无碰撞，以及 lock owner/权限/符号链接/普通单链接校验）、`Packages/MopeliumCowork/Tests/PerAgentInferenceProfileTests.swift`（attach review-await / bootstrap admission-wait exact revalidation、catalog mutation、suspended resolver、rebind/spawn TOCTOU、recovered worker frozen/live mismatch 在 provider 前 durable failure）、`Packages/MopeliumSharedUI/Tests/CoworkInferencePresentationTests.swift`；CLI offline coverage 在 `Apps/mopelium-cli/Sources/SelfTest.swift` 验证 multi-route/model/variant、旧 revision、route-scoped credentials、unique-model route 与 reasoning mismatch；explicit restore-main 当前由 `Interactive.swift` command boundary 承担，最终 CLI recovery smoke 以 `docs/TESTING.md` 为准；`Packages/MopeliumProtocol/Tests/PermissionReviewProtocolTests.swift` 覆盖 diagnostic complete-URL redaction；authorization wire/compatibility 也由该 suite 覆盖；Goal/WorkTask runtime：`WorkTaskRuntimeTests.swift`、`GoalManagerRuntimeTests.swift`、`GoalVerifierControlPlaneTests.swift`、`GoalRuntimeControllerTests.swift`；Agent completion/lease/budget/context 回归：`Packages/MopeliumAgentKernel/Tests/AgentLoopOutcomeTests.swift`、`AgentLoopPolicyTests.swift`、`ContextProjectionTests.swift`；Goal/WorkTask 协议与回放：`Packages/MopeliumProtocol/Tests/TaskGoalProtocolTests.swift`、`Packages/MopeliumConversation/Tests/TaskGoalProjectionTests.swift`，普通输入 intent 由 `ExplicitGoalIntentClassifierTests.swift` 覆盖，旧 Cowork replay 继续由 `CoworkProjectionRegressionTests.swift` 与 Protocol lease/task tests 覆盖。
- Agent session / Terminal / Shell / Git / 文档 / 媒体工具：`Packages/MopeliumTools/Sources/SessionNamingTool.swift`（严格单字段 `rename_session` schema、只描述字符数的 permission preview、宿主 current-session service seam）、`TerminalTools.swift`（runtime-owned `exec_command` / `write_stdin`、persistent process/PTY、exact owner + WorkspaceLease revalidation、跨调用输入行 guard、无法证明的编辑控制 fail closed、partial-write session teardown、bounded head+tail drain、delayed stdin echo redaction、task/runtime cleanup）、`Packages/MopeliumPTYLauncher/{MopeliumPTYLauncher.c,include/MopeliumPTYLauncher.h}`（macOS controlling PTY 与 exec-error pipe）、`ShellGit.swift`（managed terminal 与底层 runner 共用 Seatbelt/workspace/network/process cleanup helper、mandatory denied-pattern union 与大小写无关 Seatbelt deny；raw `ProcessShellRunner` 在 Linux 使用 bwrap 且缺失时 fail closed；production registry 不暴露 `run_shell`；structured backend、managed terminal 与 raw runner 分流；Git 仍走参数数组 backend）、`DocumentMediaTools.swift`（`read_pdf` / `edit_pdf_pages` / `reconstruct_document_image` / `compile_latex` / `generate_image`）、`ToolProtocol.swift`（production registry、terminal manager seam、current-session naming、structured shell、git/image-generation service）、`Packages/MopeliumProtocol/Sources/ShellCommandRisk.swift`（启动命令与交互输入共用的危险命令及 line-editor mutation 判定）、`Leases.swift`（Cowork `runShell` capability 映射到 managed terminal、不映射 raw `run_shell`，并定义 terminal mandatory credential floor）
- Managed terminal 回归：`Packages/MopeliumTools/Tests/TerminalToolsTests.swift`（真实 cwd/pipe/TTY/controlling `/dev/tty`/Ctrl-C、凭据环境、不可移除且大小写无关的凭据路径 floor、workspace/owner isolation、拆分输入、真实 zsh cursor/escape/keymap 拒绝、partial-write teardown、timeout descendant cleanup、大型 build artifact、有界输出 tail、延迟回显清洗、无人轮询自动收口、workspace replacement 后无需 poll 收口、same-sandbox signal、schema/opt-in registry）、`Packages/MopeliumAgentKernel/Tests/TerminalAgentLoopTests.swift`（durable prepare/settle 且 stdin 不落 EventLog）、`Packages/MopeliumPermission/Tests/ShellPermissionTests.swift`（`write_stdin` 不能绕过危险命令 hard deny）、`Packages/MopeliumCowork/Tests/ToolRegistryLeaseTests.swift` 与 `Packages/MopeliumProtocol/Tests/CapabilityLeaseTests.swift`（read-write worker/host exposure 与 read-only/reviewer suppression）
- Agent 网络/浏览器工具：`Packages/MopeliumTools/Sources/BrowserTools.swift`（`web_fetch` / `browser_diagnostics` / `browser_profiles` / `browser_profile_delete` / `browser_history` / `browser_navigate` / `browser_snapshot` / `browser_handoff` / `browser_reload` / `browser_back` / `browser_forward` / `browser_click` / `browser_type` / `browser_submit` / `browser_select_option` / `browser_press_key` / `browser_scroll` / `browser_wait` / `browser_screenshot` / `browser_upload_file` / `browser_download` / `browser_downloads` / `browser_search`，通过 URLSession 或已安装 Node.js + Playwright persistent context；Playwright 缺失时用 Node.js 内置 WebSocket + Chrome DevTools Protocol 驱动已安装 Chromium/Chrome/Edge profile；页面快照和动作结果返回可定位交互元素摘要，并可跟随 click/type-submit/select/submit/press 打开的新 tab/window；profile inventory 只列安全 metadata 和 runtime marker 存在性；profile 删除是显式 `.destructive` 清理工具，删除前只概括提示 runtime marker 状态；表单提交作为 exec+network browser action 暴露）、`Packages/MopeliumPermission/Sources/DeterministicPolicyGate.swift`（exec+network/destructive 权限顺序）、`Packages/MopeliumProtocol/Sources/Leases.swift`（`browse_web`）
- 权限门：`Packages/MopeliumProtocol/Sources/PermissionIntent.swift`、`ToolAuthorization.swift`（宿主解析的 concrete tool/membership/lease/args digest/preview 不可变快照；ordinary preview secret scrubber 与 diagnostic-only complete HTTP(S) URL scrubber 分离）、`Packages/MopeliumPermission/Sources/PermissionEngine.swift`、`DeterministicPolicyGate.swift`、`ModelPermissionReviewer.swift`、`SecretScanner.swift`；production Cowork 的唯一模型审查点是 `PermissionReviewControlPlane` responder，审查者只消费宿主已解析的 membership 与语义风险，不能重新解释 capability alias；非 Cowork host 才可选择 in-engine reviewer
- 事件日志与输入投影：`Packages/MopeliumConversation/Sources/EventLog.swift`（JSONL、multi-event WAL、`replayChecked` / `isEmptyChecked`、session/seq 校验）、`GoalInput.swift`（Chat/Code legacy Goal metadata 与 Cowork durable `/goal` 入口共用的语法解析）、`ExplicitGoalIntent.swift`（普通 Cowork 输入的窄、确定性中英文持续 Goal intent；不读取附件）、`Projection.swift`、`CodeProjection.swift`、`CoworkProjection.swift`
- assistant/agent 回答时间展示：`Projection.swift` / `CodeProjection.swift` 把首个对应 message envelope 的 `ts` 保留为 presentation timestamp，后续 delta/completion 只补缺不覆盖；`Packages/MopeliumSharedUI/Sources/ThreadSurfaces.swift` 提供按滚动 24 小时 / 7 天分层且遵循系统 locale/calendar/time-zone/12–24 小时偏好的公共格式，`Views.swift`、`CodeViews.swift` 与 `Apps/MopeliumMac/Sources/MopeliumChatScreen.swift` 在 agent 名称右侧消费。Cowork 复用 `CodeItemRow`，不新增 EventLog event/schema 或独立时间存储。
- Phase S session 状态：`Packages/MopeliumProtocol/Sources/SessionStateEvents.swift`（版本化完整 settings 快照、`session_settings_updated`、幂等 `session_storage_migrated`、rename source/operation ID；canonical encoding 不写 legacy `defaultProviderID`）、`Packages/MopeliumConversation/Sources/EventLog.swift` / `SessionProjectionStore.swift`（append/stream 发布落盘反解 canonical Envelope；`events.jsonl` → schema-v2 `session.json`；full-fold EventLog-wins；legacy name append-before-rebuild；exact rename retry first-write-wins、冲突 operation fail closed、A→X/B→Y/retry-A 不覆盖 Y；严格 revision helper；owner-only atomic projection）、`Packages/MopeliumCore/Sources/SessionWorkspaceAccessStore.swift`（session-owned schema-v1 binary plist、`0600`、no-follow lock、原子写、primary 默认拒删与显式事务回滚）、`Apps/MopeliumMac/Sources/Workspace.swift` / `CoworkProjectSettings.swift` / `CoworkViewModel.swift` / `MopeliumMacApp.swift`（RAII scope、shared 零引用清理、primary UI/方法/store 三层保护、scope-first symlink alias→canonical settings-before-marker migration、精确 reauthorization）、`Packages/MopeliumCowork/Sources/Orchestrator.swift`（fresh 七事件 bootstrap 与严格历史 main CAS 恢复）、`Apps/mopelium-cli/Sources/Interactive.swift`（专用 `/agent restore-main`）。对应测试：`SessionStateProtocolTests.swift`、`SessionProjectionStoreTests.swift`、`SessionRenameAgentLoopTests.swift`、`SessionNamingToolTests.swift`、`MopeliumConversationTests.swift`、`MopeliumCoreTests.swift`、`AutomaticPermissionReviewTests.swift`。
- Phase A Cowork submitted intent：`Packages/MopeliumProtocol/Sources/Event.swift` / `Task.swift`（`SubmissionID` 关联的 user/status/task additive protocol；`UserMessagePayload.mainAgentInferenceBinding` 冻结每次 Send 的 optional-schema/non-nil-new-main exact binding）、`Goal.swift`（新式 durable Goal 保存同一 binding 供 continuation/restart 使用）、`Packages/MopeliumConversation/Sources/SubmittedIntentStore.swift`（session-owned outbox、canonical `user_message + queued` transaction、状态机校验）、`CodeProjection.swift` / `CoworkProjection.swift`（提交状态 fold）、`Packages/MopeliumAgentKernel/Sources/ContextProjection.swift`（accepted submission 顺序与 fail-closed 归属过滤）、`Packages/MopeliumCowork/Sources/Orchestrator.swift`（FIFO、exact root retry、restored-task fence、main rebind + root/retry queue atomic admission batch）、`GoalRuntimeController.swift`（Goal create/每轮 continuation 传递 durable binding）、`Apps/MopeliumMac/Sources/CoworkViewModel.swift`（冻结 draft/附件/route/Goal/next-main binding、durable admission、main/Goal nil fail-closed、drain/retry）、`Packages/MopeliumSharedUI/Sources/CoworkViews.swift` / `ThreadSurfaces.swift`（始终可编辑的 composer、提交状态与 Retry）。附件 durability 由 `Packages/MopeliumCore/Sources/DurableOwnerOnlyFile.swift` 与 `Packages/MopeliumArtifacts/Sources/ArtifactStore.swift` 提供。对应测试：`SubmissionProtocolTests.swift`、`SubmittedIntentStoreTests.swift`、`SubmissionProjectionTests.swift`、`SubmittedIntentHistoryTests.swift`、`ContextProjectionTests.swift`、`OrchestrationReliabilityTests.swift`、`AutomaticPermissionReviewTests.swift`、`MopeliumArtifactsTests.swift`。
- Phase B permission reviewer request isolation：`Packages/MopeliumCowork/Sources/PermissionReviewControlPlane.swift`（`{reviewTaskID, nonce}` generation、provider/timeout first-terminal race、pre-dispatch 与 exact-generation terminal claim、request-owned 同步 cancellation token、active-generation retirement、late/duplicate drop、verdict/delivery 两层 cancel guard）、`AgentPermissionResponder.swift` / `Orchestrator.swift`（冻结 reviewer identity/exact binding，并逐代重新 resolve provider wrapper；direct attach 在 review 后和最终 durable-admission 线性化点前各有 caller-cancel fence；无 Orchestrator retain cycle）、`Packages/MopeliumProviders/Sources/ToolCalling.swift`（立即返回 request-owned stream 并传播 termination 的契约）、`Apps/mopelium-cli/Sources/Interactive.swift`（degraded-but-active 文案）。旧 `provider_still_stopping` failure kind 仅保留 legacy decode。对应测试：`PermissionReviewControlPlaneTests.swift`、`AutomaticPermissionReviewTests.swift`、`PermissionReviewProtocolTests.swift`，并与 ToolRegistry/AgentLoop/invocation/spawn/orchestration suites 组合回归。
- Phase C permission/turn outcome：`Packages/MopeliumProtocol/Sources/TurnOutcome.swift` 与 `PermissionReview.swift` / `Event.swift` / `Envelope.swift`（稳定 TurnID、typed call/turn outcome、failure source、approve/decline/cancel-turn action、manual/automatic mode 和 legacy-decodable correlation）；`Packages/MopeliumConversation/Sources/EventLog.swift` / `Projection.swift` / `CodeProjection.swift`（RequestID first-write、first-terminal CAS、FIFO pending 投影与 typed failure projection）；`Packages/MopeliumAgentKernel/Sources/AgentLoop.swift`、`ChatLoop.swift`（Decline call 继续、Cancel turn 中断且无伪 tool result、每轮 terminal）；`PermissionReviewControlPlane.swift`（同 RequestID duplicate/reconnect 共享 owner generation/terminal）；`ToolProtocol.swift` / `ShellGit.swift`（可信 wrapper-startup sandbox denial 分类）；`CodeViewModel.swift` / `CoworkViewModel.swift` / SharedUI permission card / CLI（结构化人工动作与自动审查不可操作状态）。DEBUG-only 离线验收入口为 `Apps/MopeliumMac/Sources/PhaseCPermissionFixtureView.swift`。对应测试：`TurnOutcomeProtocolTests.swift`、`PermissionSettlementTransactionTests.swift`、`PermissionProjectionTests.swift`、`AgentLoopOutcomeTests.swift`、`SandboxDenialOutcomeTests.swift`、`WorkspaceSandboxDenialTests.swift`、`PermissionReviewControlPlaneTests.swift`、`OrchestrationReliabilityTests.swift`。
- Phase L app/runtime lifecycle：`Apps/MopeliumMac/Sources/SessionRuntimeManager.swift`（进程级 exact `{SessionKind, SessionID}` Chat/Code/Cowork ownership、Cowork creation single-flight、runtime status/removal 发布、exact deletion drain、全局 bounded shutdown，以及按 exact session 的低频 display-name publisher/revision+seq watermark）、`MopeliumMacApp.swift`（窗口级 selection + shared manager、AppKit terminate-later/Command-Q、model-tool rename verified commit bridge）、`MopeliumMacRootView.swift`（切换不 stop、exact busy/delete、跨窗口 removal 与手工/model rename 的单行摘要更新）、`ChatViewModel.swift` / `CodeViewModel.swift` / `CoworkViewModel.swift`（idempotent shutdown、operation admission/drain、permission/subscription/workspace release）、`Packages/MopeliumCore/Sources/BoundedSessionRuntimeShutdown.swift`（concurrent stop、monotonic deadline、settled/timed-out report、single-flight）、`GoalRuntimeController.swift` 与 CLI `Interactive.swift`（冷启动只 reconcile/pause，显式 Resume/data-plane boundary）。DEBUG-only 离线验收入口为 `PhaseLSessionLifecycleFixtureView.swift`。对应测试：`BoundedSessionRuntimeShutdownTests.swift`、`GoalRuntimeControllerTests.swift`；Computer Use 矩阵见 `docs/TESTING.md`。
- Phase T tool outcome / task-local recovery：`Packages/MopeliumProtocol/Sources/ToolExecution.swift` / `ToolExecutionRejection.swift` 定义 additive effect disposition 与 typed no-effect rejection；`Packages/MopeliumConversation/Sources/CoworkProjection.swift` / `EventLog.swift` 负责 exact prepare/settlement、重复 ID/冲突终态 fail-closed 与 complete-known history；`Packages/MopeliumAgentKernel/Sources/AgentLoop.swift` 原子记录 model-visible failure + no-effect settlement；`Packages/MopeliumCowork/Sources/WorkTaskTools.swift` / `Orchestrator.swift` / `GoalRuntimeController.swift` 负责 production stale proof、狭窄 legacy repair、whole-task retry 和 Goal startup/进程内 launch 围栏。对应测试：`ToolExecutionProtocolTests.swift`、`ToolExecutionProjectionTests.swift`、`AgentLoopPolicyTests.swift`、`WorkTaskRuntimeTests.swift`、`GoalRuntimeControllerTests.swift`、`OrchestrationReliabilityTests.swift`。
- 对话内容渲染：`MopeliumMessageRendererMode.swift`（无偏好默认 `.microsoft`，未知值 fail closed `.plainSafe`，固定持久键，新/旧启动 override 和旧 `rich` 迁移）、`MopeliumMessageContentView.swift`（角色/mode facade、facade-lifetime raw projection、仅 exact current revision 显示上游 `DocumentView`、安全链接）、`MopeliumMicrosoftMarkdownPipeline.swift`（64 KiB admission、50 ms incomplete parse debounce、100 ms raw fixed-window leading/trailing throttle、activation/reentry/correction/truncation/final exact flush、timer generation guard、view 内 `bufferingNewest(1)`、MainActor document ownership、appearance/config/stale guard、图片/animation/citations 关闭）、`MopeliumLatestOnlyPermitScheduler.swift`（output-free、全局 concurrency 1 / pending 32、每 key latest-only/fair permit；不保存 work/result/document）。vendor 的公式面由 `MathRenderConfig.swift`、`InlineMathCatalog.swift`、`MathAttachmentData.swift`、`InlineMathPreprocessor.swift`、`UI/Paragraph/InlineMathAttachment.swift`、Markdown parser/config/inline bridge 和两平台 paragraph host 组成；只处理 code-aware 普通 `$...$`，原始 TeX 随 attachment 保留，`$$...$$` / `\(...\)` / `\[...\]` 仍为 literal。接入点为 macOS/iOS Settings（Picker 也将 legacy `rich` resolved 为 Microsoft）、macOS `RendererFixtureView`、`MopeliumChatScreen.swift` 与共享 `{Views,CodeViews,CoworkViews}.swift`；旧 `MopeliumRenderDocument.swift` / `MopeliumCodeBlockView.swift` / `MopeliumMathView.swift`、旧 regex `LaTexPreProcessor.swift` 和 highlight.js/CSS 资源仍保持删除。
- Code/Cowork execution trace 展示策略：`Packages/MopeliumConversation/Sources/CodeProjection.swift`（用 per-agent active `{TaskID, attempt}` + 该 invocation 最后一个完整 message index 判定 exact `task_completed` 镜像；迟到旧 attempt 不清除新 attempt，不匹配时保留 conversation fallback）、`Packages/MopeliumSharedUI/Sources/ExecutionTracePresentation.swift`（默认 conversation-only，后台 `-MopeliumShowExecutionTrace` / `MOPELIUM_SHOW_EXECUTION_TRACE=1` 恢复完整 trace）、`CodeViews.swift` / `CoworkViews.swift`（过滤后的 items 才进入 thread、auto-scroll、thinking 与 Code inspector）。测试为 `Packages/MopeliumConversation/Tests/MopeliumConversationCodeTests.swift` 与 `Packages/MopeliumSharedUI/Tests/ExecutionTracePresentationTests.swift`。
- iOS 预渲染救援资源：`Apps/MopeliumiOS/Resources/Settings.bundle/Root.plist` 通过系统 Settings 写入同一 renderer key，values 为 `microsoft` / `plainSafe` 且默认 Microsoft；`project.yml` 将该目录只加入 iOS app resource phase。最终 `MopeliumiOS.app/Settings.bundle/Root.plist` 必须存在，plain-safe 仍须可在打开问题 session 前选择。
- 开源声明入口：`NOTICE.md`、`ThirdPartyNotices/MarkdownRendering.md`、`ThirdPartyNotices/SyntaxHighlighting.md`、`ThirdPartyNotices/MathRendering.md`；`Packages/MopeliumSharedUI/Sources/ThirdPartyNoticesView.swift` 在设置 UI 暴露声明，`project.yml` 将根 NOTICE 与三份 notice 复制到 macOS/iOS app bundle。
- 渲染测试/fixture：`MarkdownSchedulerTests.swift`、`MessageRendererModeTests.swift`、`MessageRenderingTests.swift` 与 vendor formula/parser/platform suites 共同覆盖模式、背压、raw/final、delimiter/code/currency/fallback、attachment source、两平台 TextKit 2 paragraph/live-provider integration、Dynamic Type 与 semantic appearance；既有 vendored AppKit/UIKit stable-width invalidation 与 `DocumentView`/ParagraphView equality/selection contracts 继续保留。当前 `MessageRenderingTests` 25/25，vendor full 75 XCTest + 7 Swift Testing = 82/82。AppKit `ParagraphNSView` 还冻结 TextKit 2 root content-storage retention、wholesale replacement 后 primary-manager restoration 和 coalesced viewport layout；`InlineMathAttachment` 冻结专用动态 UTI、direct provider 与无 generic attachment cell。`Tests/Fixtures/incident-1249-sanitized-v1.json` 是脱敏的 17-message / 1,249-delta 固定样本，SHA-256 `fb548849d0b708d31e8c6d055805f29f5c09ee4c8306bf9adc537a48e95707f1`，不得因公式功能改写；validation-only host 提供 `math-one`、`math-thirty-two`、`math-structure`、`math-history`、`math-stream` stage，watchdog 每次必须显式指定 `--math disabled|single-dollar`。2026-07-24 同一 Microsoft renderer 的 structure A/B、1/32/history/stream isolation 与 Light/Dark Computer Use 均通过并无残留；双端最终 bundle/notice/font inventory也已通过。2026-07-18 三实例事故仍以根因 `UNKNOWN` 的历史 evidence 保留；短时结果不等于 >160 秒 soak、真实 clipboard/VoiceOver 或低端真机通过，plain-safe 也不能冒充 no-math baseline。
- macOS UI 信息架构：`Apps/MopeliumMac/Sources/MopeliumMacRootView.swift`（系统 `NavigationSplitView` sidebar 内的 `Mopelium` 标题、带 SF Symbol 的 Chat/Code/Cowork 竖向三行导航且仅当前项使用 interactive Liquid Glass、mode history/New、底部 Settings；session display name 解析；新建 Cowork session 选择主 workspace）、`Apps/MopeliumMac/Sources/MopeliumChatScreen.swift`（session-title Chat header；正常 assistant/agent 回复无外层卡片并使用完整内容列宽；用户消息保留 trailing 气泡 cap/gutter；无通用 agent 头像/Agent badge；名称旁稳定消息时间；composer 第一排 40pt、关闭态仅模型名的 model/variant glass 菜单左、usage 右）、`Packages/MopeliumSharedUI/Sources/CodeViews.swift`（紧凑 session-title Code shell + 原生 inspector；Code/Cowork 共用正常 agent 无框全宽正文、名称旁时间与 user/失败/结构化卡片分流）、`Packages/MopeliumSharedUI/Sources/CoworkViews.swift`（紧凑 session-title Cowork shell + 宽屏原生 project/agent/Goal/Tasks inspector；窄屏或 inspector 隐藏时不复制 Goal/Tasks；两排 composer 与可选 stop）、`Apps/MopeliumMac/Sources/MopeliumMacApp.swift` / `AppInferenceCatalog.swift` / `CoworkViewModel.swift` / `CoworkProjectSettings.swift`（Cowork 无常驻 reviewer 顶部横幅；第一排 secret-free exact profile 菜单与 Chat/Code 共用 40pt interactive Liquid Glass 几何，关闭态仅显示 model title；忙时仍可暂存下一次 `@main`，Send 冻结、FIFO 执行边界才 main-only durable rebind；第二排保留既有附件入口并使用共享 40pt control geometry；future-agent default、其他 ordinary agent rebind 与异常恢复入口位于 Project Settings）、`Packages/MopeliumSharedUI/Sources/ThreadSurfaces.swift`（Recent 旁 30×30 原生圆形 glass `+`、原生圆形 icon controls、40pt composer control/selection-menu contract、两排 composer、首排 usage、session presentation、消息时间格式与 role-aware message-width policy reusable surfaces；单行等高、多行底边对齐、无 top accessory 时不生成空行）、`Packages/MopeliumSharedUI/Sources/ProviderModelMenu.swift`（弹出菜单内部由 Chat/Code/Cowork 共用 provider 分组、真实 model ID + optional presentation variant identity、模型名与灰色 detail；关闭态按钮不复用这些辅助信息）
- 当前 UI 配色规范：`docs/CURRENT_UI_COLOR_SYSTEM.md`（系统原生表面 + Liquid Glass：动态 macOS window / sidebar、正常 assistant/agent 正文继承 canvas、结构化内容 Material、导航与交互功能层玻璃、系统语义色、iOS 边界与验收清单）
- 上一版 UI 配色底稿：`docs/UI_COLOR_SYSTEM.md`（保留迁移前 macOS 香槟金/暖中性色与玻璃体系、明暗模式、语义状态色及 iOS 差异，供历史对照）
- GUI token/turn stats：`Packages/MopeliumProtocol/Sources/TurnStats.swift`、`Packages/MopeliumProviders/Sources/ChatProvider.swift`（`Usage`）、`Packages/MopeliumProviders/Sources/OpenAIWireProvider.swift` / `OpenAIToolCalling.swift`（OpenAI-compatible usage parsing）、`Packages/MopeliumConversation/Sources/CodeProjection.swift`（`TurnStatsProjection`）、`Packages/MopeliumSharedUI/Sources/ThreadSurfaces.swift`（`MopeliumTurnStatsSummaryView`）、`Packages/MopeliumSharedUI/Sources/ChatViewModel.swift`、`Apps/MopeliumMac/Sources/CodeViewModel.swift`、`Apps/MopeliumMac/Sources/CoworkViewModel.swift`
- Chat/Code/Cowork session/history：`Packages/MopeliumCore/Sources/SessionKind.swift`（`SessionSummary` / `SessionHistoryStore`，读取 EventLog-derived schema-v2 `session.json` 与执行安全删除）、`Apps/MopeliumMac/Sources/MopeliumMacRootView.swift`（mode history、右键 Rename/Delete）、`Apps/MopeliumMac/Sources/MopeliumMacApp.swift`、`Apps/MopeliumiOS/Sources/MopeliumiOSApp.swift`。macOS 与 iOS 共享最近会话扫描和 `events.jsonl` / `artifacts` 路径生成；平台层只传不同 root 与 `SessionID`。手工 Rename 与 Code/Cowork `@main` 的 `rename_session` 都先追加 EventLog settings event 再刷新 projection，不改变目录名、`SessionID` 或既有 envelope；model schema 不接受目标 session 字段。Cowork 的 `CoworkProjectSettings.swift` 负责 GUI projection 与 legacy UserDefaults migration；canonical project/workspace/future-agent exact default/default permission/token budget metadata 已由 session settings events 持久化，bookmark bytes 独立保存在 session-owned `workspace-access.plist`。
- Provider：`Packages/MopeliumProviders/Sources/Endpoints.swift`（Chat/Code 兼容路径按 model 保存任意 JSON `modelRequestOptions`，旧 Codable 缺字段默认空）、`InferenceCatalog.swift` / `InferenceCatalogStore.swift`（Cowork versioned immutable connection/profile catalog + explicit durable option schema）、`ModelConfigurationPresentation.swift`（只读识别原始 reasoning/thinking effort/level/budget 供 UI 展示，不参与请求）、`OpenAIWireProvider.swift` / `OpenAIToolCalling.swift`（Chat/Agent 通用扩展 body 合并、runtime structural fields、config usage/candidate fields 清除与 host-owned `n = 1`）、`ProviderRegistry.swift`（兼容全局角色 provider + exact agent inference resolver）
- GUI/CLI Cowork 恢复与自动权限审查：`Apps/MopeliumMac/Sources/CoworkViewModel.swift`（session replay → orchestrator restore，并保持 persistent scheduler suspension → `@main` exact resolution + 控制面 identity/binding freeze、permission reviewer per-generation provider resolution/reviewer health → GoalRuntime start/recovery/reconcile-only pause → `startNewTasksKeepingRestoredTasksPaused`；历史 root task 只有精确 submission Retry 才解除；普通 unresolved worker 只在自己的 invocation provider dispatch 前 durable fail closed；per-agent provider resolution、安全 resolution state、host rebind；submitted intent FIFO、普通 turn scoped run、durable `/goal`、Goal/WorkTask projection、pause/resume/edit/clear、permission FIFO、cancel/retry、async stop 先 checkpoint Goal 再 `cancelAll`）、`Apps/MopeliumMac/Sources/MopeliumMacApp.swift` / `AppInferenceCatalog.swift`（catalog 编译/持久化/refresh、raw variant key → opaque durable variant ID、CoworkShell submission/Goal/Tasks/profile binding、Goal edit sheet、clear confirmation、窗口级 selection + shared `AppSessionRuntimeManager`、Command-Q terminate-later）、`Apps/MopeliumMac/Sources/MopeliumMacRootView.swift`（session/mode transition generation guard；切换只改选择并取得 manager-cached runtime，不 stop；exact busy/delete/removal）、`Apps/mopelium-cli/Sources/CLIProviderCatalog.swift` / `CLIConfig.swift`（Mopelium-owned OpenCode-compatible multi-route/model/variant config、unique unqualified model route、reasoning selection fail-closed、exact per-route credential refs）、`CLIInferenceProfiles.swift`（全部 route profile 编译、immutable catalog、safe profile selection、control-plane freeze）、`SelfTest.swift`（offline two-route/model/variant、retained revision、credential isolation）、`Interactive.swift`（per-workspace durable Cowork log；strict exact binding；仅 `@main`/control plane startup gate，worker invocation 隔离；non-empty missing `@main` 不自动套 default，显式 `/agent restore-main <path> <profile-id>`；`/profiles`、`/profile`、`/agent add ... --profile`、`/agent profile|rebind`；先 restore/GoalRuntime reconcile-only start，只有显式 `/auto|/default` 或 `/goal resume` 才恢复对应 data plane；`/goal <objective>|status|pause|resume|clear|edit`、退出/切 mode 前 Goal checkpoint + `cancelAll`、Code/Cowork image-generation service 注入）、`Apps/mopelium-cli/Sources/Terminal.swift`（渲染 `permission_review`）
- Cowork macOS project mode：`Apps/MopeliumMac/Sources/CoworkProjectSettings.swift`（EventLog-backed settings projection、legacy UserDefaults ownership/migration、future-agent exact default + settings/rebind sheet；异常时显示 workspace reauthorization / automatic-review retry Recovery 区）、`Apps/MopeliumMac/Sources/CoworkViewModel.swift`（恢复后先注册 EventLog stream；fresh settings-first seven-event bootstrap；historical main repair before reviewer replacement；`SubmittedIntentStore` outbox/canonical acceptance、FIFO drain/retry；`projectSettings` / `project` 与 durable submission/Goal/WorkTask/inference projection、GUI no-mention 默认路由到 `@main`、project directory metadata add/remove）、`Apps/MopeliumMac/Sources/Workspace.swift`（workspace picker、session-owned bookmark restore、RAII `WorkspaceAccessLease`）、`Packages/MopeliumSharedUI/Sources/CoworkViews.swift`（composer 始终可编辑，Send 只按本地 draft/同次 persistence gate；submission status/retry；roster 安全 profile label/resolution；宽屏可见 inspector 才显示 `Git Status` / `Agents` / `Goal` / `Tasks`，窄屏或 inspector 隐藏时不显示 Goal/Tasks 顶部副本；Goal pause/resume/edit/clear 与 WorkTask criteria/result/evidence/dependency/invocation detail；默认不提供用户手动新建 agent 入口）
- macOS Agent 文档/媒体/网络工具接入：`Apps/MopeliumMac/Sources/CodeViewModel.swift`（Code AgentLoop 注入 provider-backed image generation）、`Apps/MopeliumMac/Sources/CoworkViewModel.swift`（Cowork Orchestrator 按 agent 注入 provider-backed image generation；coordinator lease 可用全部文档/媒体和网络/浏览器工具，worker 默认只读 PDF 且无 `browse_web`）
- GUI provider/model catalog：`Apps/MopeliumMac/Sources/AppConfig.swift`、`Apps/MopeliumiOS/Sources/IOSConfig.swift`（provider 保存 Base URL、Chat endpoint 与非秘密的 API-key 环境变量名；model 保存 id 与展示名；macOS 外部配置额外把 `models.<id>.options` 与 `variants` 保真到内存，不写 UserDefaults；variant 作为同一 model 的请求参数预设，选择时只保存 identity 并以原始字段覆盖基础 options；当前聊天选择另存 `mopelium.providerSelection.v1`，可覆盖高级 JSON 顶层 `model`；macOS 支持 Mopelium-owned JSON/JSONC catalog，但 `options.apiKey` 只接受 `{env:NAME}`，旧明文/file/auth/providerConfig 引用不会成为生产凭据）；设置 UI：`MopeliumSettingsPanel`（macOS）与 `IOSRootView.settingsSheet`（iOS）只编辑环境变量名；Chat 模型菜单：`MopeliumChatScreen`（macOS）、`IOSRootView` toolbar（iOS）；环境变量 resolver：`Apps/MopeliumMac/Sources/Keychain.swift`、`Apps/MopeliumiOS/Sources/Keychain.swift`（历史文件名；真实请求只解析 `.environment`，不读写 OS Keychain、auth JSON 或 secret file）
- Cowork 设计文档：`docs/COWORK_AGENT_ARCHITECTURE.md`、`COWORK_TASK_CONTEXT_MODEL.md`、`COWORK_AGENT_INVOCATION_MODEL.md`、`COWORK_CURRENT_FINDINGS.md`、`COWORK_MIGRATION_PLAN.md`、`COWORK_V0_10_SMOKE.md`、`COWORK_V0_10_STATUS.md`；当前 per-agent inference durable 契约：`docs/PER_AGENT_INFERENCE_PROFILES.md`；AI 配置写入 runbook：`docs/AI_PROVIDER_MODEL_CONFIGURATION.md`
- 开源复用政策：`docs/OPEN_SOURCE_REUSE.md`（允许的复用形式、许可证准入、provenance、NOTICE、Apple-first 集成、OpenCode 当前 research-only 状态与上游升级规则）

## 生成物 / 产物

- SwiftPM 构建产物：`.build/`（含 release 可执行）
- Xcode 工程产物：`Mopelium.xcodeproj`（xcodegen 生成，gitignored）
- App bundle：`MopeliumMac.app` / `MopeliumiOS.app`（Xcode 构建产物）

## 脚本与工具

| 脚本 | 用途 | 调用方式 |
|---|---|---|
| `Makefile` | build/test/release/install/app/clean 便利 target | `make build` / `make test` / `make app` 等 |
| `project.yml` | XcodeGen 工程规格 | `xcodegen generate`（`make app` 内调用） |

## 不确定项

- Mopelium 仓与 Councis 仓共享同一 `ARCHITECTURE.md` 与 `Packages/` 结构。二者关系（Councis 是 Mopelium 的 CLI 原型分支？独立产品？）`UNKNOWN` — 需用户确认。
- `Apps/mopelium-cli/Sources/Interactive.swift` REPL 是否接入 `main()` `UNKNOWN`（Councis 仓调研显示疑似死代码，Mopelium 仓需独立确认）。
